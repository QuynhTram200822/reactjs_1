import Image from "next/image";
import styles from "./contact.module.css";

export default function Contact() {
  return (
    <div className={`${styles.container} `}>
      <h3 className={`${styles.container__title}`}>Contact with me!</h3>
    </div>
  );
}
